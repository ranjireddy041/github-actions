name: first github action

on: 
  push:
    branches:
      -main
jobs:
 practice:
    runs-on: ubuntu-latest

    steps:
       - name: Checkout code
         uses: actions/checkout@v4
         
       - name: Run a script
         run: echo "Hello GitHub Actions"
